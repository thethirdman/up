Chargen -- Character Generator Protocol
===================================

Chargen is an implementation of the RFC 864

Requirements
------------

You need an ocaml compiler, with ocamlfind

Running Chargen
---------------

You can just launch the executable. By default, it will run using tcp.
You can also specify the protocol, either '--tcp' or '--udp'

        ./chargen --udp
